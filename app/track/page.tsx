import Link from"next/link";import{trackGuestRequest}from"@/app/actions/guest-requests";import{TrackingForm}from"@/components/requests/tracking-form";
export const metadata={title:"Track transport request | AFOS"};export default async function TrackPage({searchParams}:{searchParams:Promise<{reference?:string;token?:string}>}){const p=await searchParams;return <main className="public-request-page track-page"><nav className="simple-public-nav"><Link className="brand" href="/"><span className="brand-mark">A</span><span><strong>AFOS</strong><small>African Freight Operating System</small></span></Link><Link href="/request">New request</Link></nav><header><span>SECURE REQUEST TRACKING</span><h1>See what is happening.</h1><p>Use the reference and private tracking code created when the request was submitted.</p></header><TrackingForm action={trackGuestRequest} reference={p.reference} token={p.token}/></main>}
