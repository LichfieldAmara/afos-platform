"use client";
import {useActionState} from "react"; import {useFormStatus} from "react-dom"; import type {OpsState} from "@/app/actions/operations";
type Action=(s:OpsState,f:FormData)=>Promise<OpsState>; type Item={id:string;label:string};
function Submit({children}:{children:React.ReactNode}){const{pending}=useFormStatus();return <button className="form-submit" disabled={pending}>{pending?"Saving…":children}</button>}
function Message({state}:{state:OpsState}){return state.message?<p role="status" className={`form-message ${state.success?"form-success":"form-error"}`}>{state.message}</p>:null}
export function CapacityForm({action,providers}:{action:Action;providers:Item[]}){const[s,a]=useActionState(action,{message:""});return <form action={a} className="ops-form"><label><span>Verified provider</span><select name="providerId" required defaultValue=""><option value="" disabled>Select provider</option>{providers.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label><span>Truck registration</span><input name="truck" required/></label><label><span>Trailer registration</span><input name="trailer" required/></label><label><span>Driver name</span><input name="driver" required/></label><label><span>Driver phone</span><input name="phone" inputMode="tel" required/></label><label><span>Driver licence number</span><input name="license" required/></label><label><span>Licence expiry <small>Optional</small></span><input name="expiry" type="date"/></label><label><span>Container size</span><input name="size" list="capacity-sizes" placeholder="Example: 20 foot" required/><datalist id="capacity-sizes"><option value="10 foot"/><option value="20 foot"/><option value="40 foot"/><option value="45 foot"/></datalist></label><label><span>Number available</span><input name="quantity" type="number" min="1" max="100" defaultValue="1" required/></label><label><span>Available from</span><input name="from" type="datetime-local" required/></label><label><span>Available until</span><input name="until" type="datetime-local" required/></label><label><span>Pickup area <small>Optional</small></span><input name="pickup"/></label><label><span>Destination area <small>Optional</small></span><input name="destination"/></label><label className="wide"><span>Notes <small>Optional</small></span><textarea name="notes" rows={2}/></label><Message state={s}/><div className="wide"><Submit>Save available capacity</Submit></div></form>}
export function OfferForm({action,requests,capacity}:{action:Action;requests:Item[];capacity:Item[]}){const[s,a]=useActionState(action,{message:""});return <form action={a} className="ops-form"><label><span>Transport request</span><select name="requestId" required defaultValue=""><option value="" disabled>Select request</option>{requests.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label><span>Suitable capacity</span><select name="capacityId" required defaultValue=""><option value="" disabled>Select verified capacity</option>{capacity.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label><span>Containers offered</span><input name="quantity" type="number" min="1" defaultValue="1" required/></label><label><span>Response needed by</span><input name="expires" type="datetime-local" required/></label><Message state={s}/><div className="wide"><Submit>Send provider offer</Submit></div></form>}
export function ResponseForm({action,offerId}:{action:Action;offerId:string}){const[s,a]=useActionState(action,{message:""});return <form action={a} className="inline-action"><input type="hidden" name="offerId" value={offerId}/><select name="decision"><option value="accepted">Provider accepted</option><option value="rejected">Provider rejected</option></select><input name="reason" placeholder="Reason if rejected"/><Message state={s}/><Submit>Record response</Submit></form>}
export function AllocationForm({action,offers,trucks,trailers,drivers}:{action:Action;offers:Item[];trucks:Item[];trailers:Item[];drivers:Item[]}){const[s,a]=useActionState(action,{message:""});return <form action={a} className="ops-form"><label><span>Accepted offer</span><select name="offerId" required defaultValue=""><option value="" disabled>Select offer</option>{offers.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label><span>Truck</span><select name="truckId" required defaultValue=""><option value="" disabled>Select truck</option>{trucks.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label><span>Trailer</span><select name="trailerId" required defaultValue=""><option value="" disabled>Select trailer</option>{trailers.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label><span>Driver</span><select name="driverId" required defaultValue=""><option value="" disabled>Select driver</option>{drivers.map(x=><option key={x.id} value={x.id}>{x.label}</option>)}</select></label><label><span>Trip starts</span><input name="start" type="datetime-local" required/></label><label><span>Expected finish</span><input name="end" type="datetime-local" required/></label><Message state={s}/><div className="wide"><Submit>Allocate and create trip</Submit></div></form>}
